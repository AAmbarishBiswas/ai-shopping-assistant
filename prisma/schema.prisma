generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url     = env("DATABASE_URL")
}

model Wishlist {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  productId Int
  name      String
  price     Int
  rating    Float
  userId    String
}